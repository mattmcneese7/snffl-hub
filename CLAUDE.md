# Squirtnite FFL Hub

The approved execution brief (docs/BRIEF.md) is the single source of truth. Read it fully at the start of every session.

## Rules
1. Work one checkpoint at a time from Brief Section 5. Before each checkpoint, reply with scope, files to touch, and a token estimate, then stop and wait for Matt's approval.
2. Build only what the brief describes. If something is unclear or missing, ask instead of guessing.
3. Never commit API keys or secrets. The repo is public. Keys live in GitHub Actions secrets and Vercel env vars only; local `.env*` files stay gitignored.
4. Never use em dashes or double hyphens in any site copy or generated text.
5. Run the contrast checks in both themes (light and dark) before showing any screen.

## Quick facts
- Sleeper league ID: `1394336593518546944`
- Stack: Next.js on Vercel Hobby, GitHub Actions for schedules, Supabase free tier, Claude API ($10 credit)
